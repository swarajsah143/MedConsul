# Google Sign-In — same OAuth 2.0 Web application Client ID configured in the repo-root
# .env as GOOGLE_CLIENT_ID (the value is public; it identifies the app to Google, it is
# not a secret). Copy this file to client/.env and fill it in, or leave blank to hide the
# "Continue with Google" button.
VITE_GOOGLE_CLIENT_ID=
